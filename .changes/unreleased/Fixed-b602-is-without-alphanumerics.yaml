kind: Fixed
body: 'An `is` comparison between values that contain no ASCII letter or digit (`!!!`, `é€`) is now evaluated: it always failed, so a confirmation made only of symbols or non-ASCII letters could never pass. Pickup: restart AND re-bake. (#B602)'
time: 2026-09-24T00:12:53+01:00
